name: IndexedDB.NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run: dotnet restore src/IndexedDB.NET/IndexedDB.NET.csproj
    - name: Build
      run: dotnet build src/IndexedDB.NET/IndexedDB.NET.csproj -c Release